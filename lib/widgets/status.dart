import 'package:flutter/material.dart';

class StatusAtleta extends StatelessWidget {
  final int codStatus;
  final IconData? icone;


  StatusAtleta(this.codStatus, {this.icone});


  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
